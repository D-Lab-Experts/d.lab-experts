---
import Layout from '../../layouts/Layout.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations();
---

<Layout
  title={t('pageTitles.notFound')}
  description={t('pageTitles.notFoundDescription')}
>
  <section class="nf-hero">
    <div class="section-glow lilac" style="top:0;left:50%;transform:translateX(-50%);width:500px;height:400px"></div>
    <div class="section-glow blue" data-parallax="0.08" style="bottom:0;right:-80px"></div>
    <div class="container">
      <div class="nf-inner fi">
        <div class="nf-code">
          <span class="gradient-text">404</span>
        </div>
        <h1 class="nf-title">{t('notFoundPage.title')}</h1>
        <p class="nf-sub">{t('notFoundPage.sub')}</p>
        <div class="nf-ctas">
          <a href="/" class="btn btn-primary">{t('notFoundPage.ctaHome')}</a>
          <a href="/blog" class="btn btn-ghost">{t('notFoundPage.ctaPublications')}</a>
        </div>

        <div class="nf-search-hint">
          <div class="label" style="margin-bottom:12px">{t('notFoundPage.shortcutsLabel')}</div>
          <ul class="nf-links">
            <li><a href="/#segments">{t('notFoundPage.shortcutAgenda')}</a></li>
            <li><a href="/#products">{t('notFoundPage.shortcutProducts')}</a></li>
            <li><a href="/about">{t('notFoundPage.shortcutAbout')}</a></li>
            <li><a href="/blog">{t('notFoundPage.shortcutPublications')}</a></li>
            <li><a href="/rss.xml">{t('notFoundPage.shortcutRss')}</a></li>
          </ul>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .nf-hero {
    padding: 180px 0 140px;
    position: relative;
    z-index: 1;
    overflow: hidden;
    min-height: calc(100vh - var(--nav-h) - 200px);
    display: flex;
    align-items: center;
  }
  .nf-inner {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 760px;
    margin: 0 auto;
  }
  .nf-code {
    font-size: clamp(110px, 18vw, 200px);
    font-weight: 900;
    letter-spacing: -0.05em;
    line-height: 1;
    margin-bottom: 24px;
    opacity: 0.95;
  }
  .nf-title {
    font-size: clamp(28px, 3.4vw, 38px);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1.12;
    margin-bottom: 18px;
  }
  .nf-sub {
    font-size: 17px;
    color: var(--muted);
    line-height: 1.7;
    max-width: 520px;
    margin: 0 auto 40px;
  }
  .nf-ctas {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 56px;
  }
  .nf-search-hint {
    border-top: 1px solid var(--border);
    padding-top: 36px;
    max-width: 420px;
    margin: 0 auto;
  }
  .nf-links {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px 24px;
    list-style: none;
    padding: 0;
    margin: 0;
    text-align: left;
  }
  .nf-links a {
    font-size: 14px;
    color: var(--muted);
    transition: color 0.2s;
    display: inline-block;
    padding: 4px 0;
  }
  .nf-links a:hover {
    color: var(--lilac);
  }
  @media (max-width: 640px) {
    .nf-links { grid-template-columns: 1fr; }
    .nf-ctas { flex-direction: column; align-items: stretch; }
    .nf-ctas .btn { justify-content: center; }
  }
</style>
