---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Página não encontrada | D.Lab"
  description="A página que você procurou não existe ou foi movida. Voltar para a home do D.Lab ou explorar as pesquisas públicas."
>
  <section class="nf-hero">
    <div class="section-glow lilac" style="top:0;left:50%;transform:translateX(-50%);width:500px;height:400px"></div>
    <div class="section-glow blue" style="bottom:0;right:-80px"></div>
    <div class="container">
      <div class="nf-inner fi">
        <div class="nf-code">
          <span class="gradient-text">404</span>
        </div>
        <h1 class="nf-title">Esta página não existe.</h1>
        <p class="nf-sub">
          A URL que você acessou pode ter sido movida, renomeada ou talvez nunca tenha
          existido. Mas temos bastante conteúdo pra você explorar.
        </p>
        <div class="nf-ctas">
          <a href="/" class="btn btn-primary">Voltar para a home →</a>
          <a href="/blog" class="btn btn-ghost">Ver pesquisas públicas</a>
        </div>

        <div class="nf-search-hint">
          <div class="label" style="margin-bottom:12px">Atalhos</div>
          <ul class="nf-links">
            <li><a href="/#segments">Segmentos de atuação</a></li>
            <li><a href="/#products">Produtos e serviços</a></li>
            <li><a href="/about">Sobre o D.Lab</a></li>
            <li><a href="/blog">Pesquisas públicas</a></li>
            <li><a href="/rss.xml">RSS das pesquisas</a></li>
          </ul>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .nf-hero {
    padding: 180px 0 140px;
    position: relative;
    z-index: 1;
    overflow: hidden;
    min-height: calc(100vh - var(--nav-h) - 200px);
    display: flex;
    align-items: center;
  }
  .nf-inner {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 760px;
    margin: 0 auto;
  }
  .nf-code {
    font-size: clamp(110px, 18vw, 200px);
    font-weight: 900;
    letter-spacing: -0.05em;
    line-height: 1;
    margin-bottom: 24px;
    opacity: 0.95;
  }
  .nf-title {
    font-size: clamp(28px, 3.4vw, 38px);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1.12;
    margin-bottom: 18px;
  }
  .nf-sub {
    font-size: 17px;
    color: var(--muted);
    line-height: 1.7;
    max-width: 520px;
    margin: 0 auto 40px;
  }
  .nf-ctas {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 56px;
  }
  .nf-search-hint {
    border-top: 1px solid var(--border);
    padding-top: 36px;
    max-width: 420px;
    margin: 0 auto;
  }
  .nf-links {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px 24px;
    list-style: none;
    padding: 0;
    margin: 0;
    text-align: left;
  }
  .nf-links a {
    font-size: 14px;
    color: var(--muted);
    transition: color 0.2s;
    display: inline-block;
    padding: 4px 0;
  }
  .nf-links a:hover {
    color: var(--lilac);
  }
  @media (max-width: 640px) {
    .nf-links { grid-template-columns: 1fr; }
    .nf-ctas { flex-direction: column; align-items: stretch; }
    .nf-ctas .btn { justify-content: center; }
  }
</style>
